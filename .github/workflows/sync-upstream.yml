name: sync-upstream
on:
  schedule:
    - cron: '0 6 * * *'  # ogni giorno alle 07:00 italiane
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: |
          git remote add upstream https://github.com/ahmeabd/italia-corpus.git
          git fetch upstream
          if git merge upstream/main --no-edit; then
            git push origin main
          else
            echo "Merge conflittuale — skip push e dataset rebuild"
            git merge --abort
            exit 1
          fi
      - name: Trigger dataset rebuild
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run build-dataset.yml --ref main
